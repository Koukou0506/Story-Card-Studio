# Phase C2.1 实施计划

基线（2026-07-14，Asia/Shanghai）：`npm.cmd run typecheck` 通过；`npm.cmd test` 为 32 个文件、216 项测试全部通过；`npm.cmd run build` 通过。

1. 完成一级页面移动阻塞审计，固定 360、390、412、768、1024 和桌面验证矩阵。
2. 先为 Manifest、Service Worker、离线状态、Storage Adapter、乐观锁、冲突副本和移动壳编写失败测试。
3. 增加 IndexedDB 优先、localStorage 回退的浏览器 Storage Adapter，并迁移 v7 旧草稿。
4. 增加可选服务端工作区 Adapter、JSON 文件持久化、版本校验和备份接口。
5. 增加单用户会话、CSRF、Origin/CORS、频率和请求大小保护。
6. 增加 Manifest、图标、Service Worker、离线页、安装检测和非破坏性更新提示。
7. 将在线、保存、同步、冲突状态接入统一 App Shell，并增加移动抽屉和底部快捷导航。
8. 把全部一级页面收敛为移动单列，正文增加全屏和面板切换，文件操作增加类型/大小校验。
9. 增加大型列表分批呈现、输入保存去抖、触摸目标和焦点管理。
10. 更新 README/架构/移动文档，运行完整测试、构建、PWA 静态检查和六档视口验证。

