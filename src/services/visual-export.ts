const escapeXml = (value: unknown) => String(value ?? "").replace(/[&<>"']/g, (x) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&apos;" }[x]!));
export function exportVisualJson(value: unknown) { return JSON.stringify(value, null, 2); }
export function exportVisualCsv(rows: Array<Record<string, unknown>>) { const keys = [...new Set(rows.flatMap((row) => Object.keys(row).filter((key) => !["sources", "jump"].includes(key))))]; const cell = (value: unknown) => `"${(typeof value === "object" ? JSON.stringify(value) : String(value ?? "")).replace(/"/g, '""')}"`; return [keys.map(cell).join(","), ...rows.map((row) => keys.map((key) => cell(row[key])).join(","))].join("\n"); }
export function exportVisualSvg(title: string, rows: Array<Record<string, unknown>>) { const height = Math.max(120, 70 + rows.slice(0, 50).length * 24); return `<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="${height}" viewBox="0 0 1200 ${height}" role="img" aria-label="${escapeXml(title)}"><rect width="100%" height="100%" fill="#f7f5ed"/><text x="32" y="40" font-size="24" fill="#183b2b">${escapeXml(title)}</text>${rows.slice(0, 50).map((row, index) => `<text x="32" y="${72 + index * 24}" font-size="14" fill="#24352d">${escapeXml(row.title ?? row.id ?? "")}</text>`).join("")}</svg>`; }
export async function exportVisualPng(svg: string): Promise<Blob> { const image = new Image(); const url = URL.createObjectURL(new Blob([svg], { type: "image/svg+xml" })); await new Promise<void>((resolve, reject) => { image.onload = () => resolve(); image.onerror = () => reject(new Error("图表图片生成失败。")); image.src = url; }); const canvas = document.createElement("canvas"); canvas.width = image.naturalWidth || 1200; canvas.height = image.naturalHeight || 800; canvas.getContext("2d")?.drawImage(image, 0, 0); URL.revokeObjectURL(url); return await new Promise((resolve, reject) => canvas.toBlob((blob) => blob ? resolve(blob) : reject(new Error("浏览器不支持 PNG 导出。")), "image/png")); }
